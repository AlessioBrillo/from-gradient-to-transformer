---
tags: [type/moc, phase/6, phase/7, research/experiment, state/review]
created: 2026-08-14
---

# Micro-Phase 37 — The Sixth Question: the continuum law, sixth execution, written from the release report (conditional draft)

> **STATUS: DRAFT — NOT A PRE-REGISTRATION.** The pipeline caps at seven:
> MP-36 is the last pre-registration until the stack executes, and the law says
> the next roadmap is written from MP-36's release report, not from the habit
> of pre-registering. This file is that roadmap, drafted in advance so the
> sixth continuum choice is a stamping, never a discovery — the fork drill
> (Ex-H of MP-36) at macro scale. Nothing in this file opens a row, launches a
> run, or claims a window: it becomes binding only at MP-37's Session 0, when
> ADR-0010's eight rows are consumed as the intake table and the draft is
> rewritten from them, with every deviation recorded as a dated ledger note.
> It is not wired into home and it is not counted against the cap.

Written as a personal learning log and a public record, like every roadmap
before it — with one difference that is the point of the file: this roadmap is
written before the release report it is conditioned on, so its conditions are
conditions, never predictions, and its candidate set is frozen in this file so
Session 0 can adjudicate, never improvise.

MP-37 executes the continuum law for the sixth time, under a NEW ledger
(ADR-0011) and a NEW frozen candidate set (C21–C24, frozen in this file): the
fifth research question consumed as artifact, the paper v7 rule decided, the
sixth question chosen from C21–C24 with the unchosen closed in the same
sitting, the essay annex v7, the shelf's year five at zero, the stranger round
7, the teaching lane shipping its fifth runnable artifact with a stranger-run
transcript, the debt re-verified. What makes this execution different is not
the law — it is the intake: for the first time in the program's history, the
roadmap is written from a release report rather than from the habit of
pre-registering, and this draft is the promise the cap made, committed to disk
before the release it waits for.

What is different about the sixth time: by MP-37's Session 0 the record will
hold five dated directions, a shelf in its fifth year, a paper at v5/v6, four
teaching artifacts strangers have run, and — most decisively — MP-29's dense
characterization of the P=113 solution (its S3 contribution) on disk with
manifest-tagged numbers, and whichever of C17/C18 MP-36's sitting opened: a
sparse cell found (the redemption) or the dense→memorized boundary mapped as
an order-parameter study. The science has matured from *will grokking
reproduce?* through *which open question is deepest?* to *what does my own
phase map say about the boundary?* — the sixth question is the first one
chosen with a characterization (or its redemption) already on the record, and
this roadmap is written verdict-agnostic by law: nothing in it re-plans a
single row of MP-29–MP-36; its candidates are conditions, not predictions.

The deepest law still applies: a promise can be re-planned forever, but a
dated row is answered. This draft stands while MP-29 executes and
MP-30–MP-36 stand pre-registered, gated in series — seven phases stacked was
the exact drift the record has named since MP-18 — so Session 0 is the
mechanical refusal of it: no MP-36 release report (ADR-0010 at zero UNDECIDED
rows, the site live, `verify-claims` at 0, the fourth teaching transcript on
disk), no phase. The gate is a checklist with receipts, not a paragraph. This
is also the un-cap executed exactly once: the next roadmap is written from a
release report, and this file is its draft.

## Where this phase starts (state review, verified against the repo 2026-08-14)

The intake is MP-36's release report; below is what the record stands at the
moment of drafting, so this roadmap's conditions are auditable from day one.
MP-36's Session 8 updates every line; nothing here re-plans a single row of
it.

- **Tree state**: `dev` clean at the MP-36 pre-registration merge (PR #69,
  2026-08-13). MP-29 is mid-execution: the positive control is stamped
  ALL-DENSE (harness-level negative at P=59/67/97, val 0.0000–0.0006, gen
  −1); microscope trial 1 FALSIFIED (embedding re-normalization not the
  suppressor, k_99 = 112/113); trials 2 (`--schedule constant`, enabler
  landed TDD-first, RED 82fb216 → GREEN 1479c5d) and 3 (wd 1.5×) pending;
  the R1 standard-scale ×3-seed run COMPLETED 2026-08-14 04:07 local — the
  scheduled no-head negative is now the verdict: 0/8 heads, peak diag+1
  mass 0.075 at epoch 499 (final 0.062), peak val accuracy 0.5083 near epoch
  1950, K-composition max 0.056, row 3's analysis and stamping pending; the
  exp2 and exp5 manifests clean on disk (git_sha 9a67b10, a50f943),
  `verify-claims` at **0** re-verified in this sitting. MP-30–MP-36 stand
  pre-registered, gated in series, the cap at seven; their residue rows are
  this phase's intake, per the record's law that a session is not over while
  a row it owns is undated.
- **The ledgers**: ADR-0003 rows 1–2 stamped (NO-GROK; microscope trial 1
  FALSIFIED), rows 3–7 in MP-29's window; ADR-0004's five horizon rows
  MP-30's to decide; ADR-0005's eight premiere rows MP-31's; ADR-0006's eight
  rows MP-32's; ADR-0007's eight rows MP-33's; ADR-0008's eight rows MP-34's;
  ADR-0009's eight rows MP-35's; ADR-0010's eight rows MP-36's. **ADR-0011's
  eight rows UNDECIDED — the rows this phase's roadmap will fill**, filled
  exactly once, under the continuum law, sixth execution.
- **The scientific ledger**: no run in this repository's history has ever
  produced a sparse Fourier solution (P=59 dense 59/59; P=113 k_99 = 111/113
  val 1.0 — the NO-GROK positive-negative; control scan P=59/67/97 all-dense;
  microscope trial 1 dense 112/113; the R1 standard-scale run 0/8 heads). By
  MP-37's Session 0 this will include the microscope's remaining trials, the
  R1 head verdict, the C5–C20 adjudications, and **five research questions'
  verdicts or scheduled negatives** — including whichever of C17/C18 opened
  under ADR-0010 and the answer to whether a sparse cell exists anywhere on
  the phase diagram. The C21–C24 adjudication below is pre-conditioned on
  those verdicts, exactly as C17–C20 were on MP-29–MP-35's.
- **The CI floor**: the tracked 190+ tests, ruff, blocking mypy and
  markdownlint baselines green at MP-36's release; `verify-claims` at its
  actual count, with the phase-diagram manifest as the newest designed line.
- **The toolchains, verified 2026-08-14**: no LaTeX toolchain on this
  machine (`make paper` graceful, not green); no Pages deploy workflow in
  `.github/workflows/`; no `publish:` frontmatter policy;
  `portfolio/projects/` empty; W&B never connected; the exp5 1000-epoch
  ×3-seed re-run a recorded pending item (~15 h) that ADR-0009's row 8 and
  ADR-0010's row 8 are obligated to date. All are dated rows owned by
  MP-30/31/34/35/36 — recorded here as their residue, never this phase's
  re-planning.
- **The showcase corpus by MP-37's intake**: 12+ provenance-guarded figures;
  the paper at v5/v6; the site and Space live since the premiere; the essay
  annex v5/v6; **four runnable teaching artifacts with stranger-run
  transcripts**. MP-37's teaching lane extends this corpus to five artifacts
  a stranger can run.

## Design decisions

- **The continuum law, sixth execution.** Exactly one new research row
  (row 3) opens per phase, chosen at Session 0 from the pre-registered
  candidate set C21–C24 below; the unchosen candidates close with one dated
  reason each, in the same sitting. The session gate is unchanged: a session
  is not over while a row it is responsible for is undated; Session 8
  requires zero UNDECIDED rows. Verdict criteria are written into the row's
  cells **before** the window opens and are never edited afterwards; the
  only allowed post-launch edit is "observed".
- **A new ledger for a new law-execution.** ADR-0010 is full and closed at
  MP-36's release; closed-then-reopened is a NEW row, never a revision
  (inherited). ADR-0011 is this phase's ledger, its eight rows pre-stamped
  with windows and kill-dates at Session 0, its candidate set frozen in this
  file.
- **The candidate set is frozen here, not at S0.** The law says the set is
  pre-registered; Session 0 only adjudicates. C21–C24 are written below with
  their opens-only-if conditions on MP-29–MP-36 verdicts, so a sitting can
  decide, never improvise. The induction-head fork (C2→C6→C11→C15) is *not*
  re-included: its R1 condition has long since adjudicated, and a fifth
  re-inclusion would be a mood, not a candidate.
- **Consumption is execution, not memory (unchanged).** MP-36's row-3 verdict
  (the fifth research question's result or scheduled negative) is consumed
  into an artifact in the same sitting that owns it (row 1); the paper v7
  opens only if that artifact lands new numbers, else row 2 closes with the
  reason "the v6 is the record" — a dated sentence, never a silence.
- **The record teaches, and the receipt compounds (now five deep).** Row 7
  ships the **fifth** runnable artifact — the walkthrough v5, the 10-minute
  talk v5, or the Colab grokking notebook v3, decided at the S5 kickoff —
  because the first four artifacts' stranger-run transcripts proved the
  receipt works. The teaching register now has five entries, a documented
  delta between them, and — if C23 opens — the receipts become a measured
  rate rather than a mood.
- **The shelf is a row, not a mood (unchanged).** Row 5 carries a heartbeat,
  a hostile-webmaster walk at zero, and a dated close-or-launch — **year
  five** of the shelf begins here, with its baseline in S1, rehearsal in S7,
  release in S8. A live public number that exceeds the record is a blocked
  row, not a to-do.
- **The stranger's pipeline extends to round 7.** Row 6 carries the
  ADR-0004 human-dependency rule: intake at S4, kill-date at S5, the
  self-review substitute written in S2 from the visitor's chair. Never a
  silent skip.
- **The debt row re-verifies, it never re-does.** Row 8 re-verifies MP-36's
  stamped closures (W&B, clean-clone proof, graduation proof,
  `reproduce-multiseed` exp2/exp5, and the exp5 1000-epoch ×3-seed
  resolution that ADR-0010's row 8 is obligated to date) with dates and
  transcripts — a claimed closure without its transcript stays open, and a
  NEW debt cell lands as a NEW row, never a revision. A pending item cannot
  outlive a ledger.
- **The S0 gate is the mechanical refusal of drift (unchanged, now the
  un-cap).** Session 0 opens only on MP-36's release report: ADR-0010 at
  zero UNDECIDED rows, the site live, `verify-claims` at 0, the fourth
  teaching transcript on disk, `dev == main`. The gate is a checklist with
  receipts.
- **The terminus is stamped at Step 0**: release = this roadmap's merge + 14
  calendar days. On that date the phase ships everything the rows decided —
  the residue is a dated list, never a silence.
- **The draft is the promise the cap made (new this phase).** This file is a
  draft, not a pre-registration: it opens no rows and is not wired into
  home. Session 0 promotes it — the roadmap is rewritten from MP-36's
  release report, the first in the program's history written from a release
  report rather than from the habit of pre-registering; the draft's
  deviations are recorded like any roadmap's, as dated ledger notes.
- **Architecture laws unchanged.** `dev` only, GPG-signed, Conventional
  Commits with `(meta)`, `(portfolio)`, `(ci)` scopes; CI green before any
  merge; the floor re-verified locally before every push; zero UNDECIDED rows
  at Session 8; release = merge + 14 calendar days.

## The frozen candidate set for the sixth research question (chosen at S0)

| # | Candidate | Opens only if | Why it would close |
|---|---|---|---|
| C21 | The dense algorithm's computation (C13/C14's successor, the mechanism reading): with the dense regime characterized on disk, read the P=113 solution as code — per-head Fourier dictionaries, norm structure vs the ideal full-support DFT expansion, frequency ablation on the dense circuit, progress measures along the dense trajectory | MP-29 S3's dense characterization note is on disk with manifest-tagged numbers (the P=113 checkpoints it cites exist: `exp2_checkpoint_seed{0,1,2}.pt`) | A sparse cell exists anywhere → C17's successor owns the reading, this closes with that verdict |
| C22 | The finite-size scaling of the dense→memorized boundary (C18's successor): boundary width and critical wd vs P, the sharpening-with-P prediction written before the analysis, the Morwani et al. edge-of-numerical-stability check across cells; **analysis-only on the diagram's data plus one bounded confirmatory slice**; falsifiable predictions (monotone order-parameter curves, sharpening) written before the analysis | ADR-0010 row 3 = C18 with monotone order-parameter curves on disk | C18 never opened, or its curves not monotone → closes with that verdict |
| C23 | The stranger-run reproducibility rate, measured (C19's successor): across the teaching register's ≥ 4 stranger-run transcripts, the per-step pass/fail distribution, and whether every public claim a stranger can reach still re-derives from one command line on *their* machine; the verdict is a dated reproducibility report with a rate | The teaching register holds ≥ 4 stranger-run transcripts on disk at S0 (MP-36's row 7 ships the fourth) | Fewer than 4 transcripts → the receipt system hasn't earned a study, this closes with that reason |
| C24 | The regime-resolved SAE map, re-converted (C20's successor): L0/FVE/dead-features as a function of solution regime across the phase diagram's cells (memorized / dense / sparse-adjacent), converting the standing prediction "SAEs struggle exactly where the circuit is dense" into a regime-labeled map | The SAE-on-dense evidence lane exists (C10's verdict or MP-29 S3's reading) AND the diagram's checkpoints exist | A sparse regime exists → C21's successor owns the reading; or no SAE lane on disk → closes with that reason |

The likely survivor of this sitting: **C21** — the mechanism reading of the
dense algorithm, the record's signature arc completed: negative (no run ever
grokked) → map (the phase diagram) → characterization (MP-29's S3 note) →
mechanism (this question). It is always CPU-runnable on checkpoints that
exist today, independent of every fork except the redemption, and it converts
the record's deepest open fact from "no sparse solution" into "here is what
the dense solution actually computes." C22 is the scientific successor if
C18 delivered monotone order parameters (the boundary as a law-like
statement); C23 is the fallback (always-runnable, the showcase's own
science); C24 is the evidence lane. The S0 sitting decides with the verdicts
in front of it, exactly as the law says.

## Session 0 — the gate truthing + the continuum choice (next, ~1 h)

- **Gate**: MP-36's release report consumed row by row — ADR-0010 at zero
  UNDECIDED rows, the first live URL re-clicked in this sitting, `verify-claims`
  at its actual count, the fourth teaching transcript on disk, `dev == main`.
  The intake table is committed before a single continuum row opens; this
  draft is promoted to the roadmap in the same sitting, rewritten from the
  release report with its deviations recorded as dated ledger notes.
- **The continuum choice (the C21–C24 adjudication, decided in this sitting)**:
  each candidate's opening-or-closure memo written in three sentences with a
  falsifier; exactly one opens as row 3; the unchosen close with one dated
  reason each, stamped in the same sitting; the chosen candidate's protocol
  (site, metric, negative control, kill-date) is pre-registered in S5 before
  the first pass, exactly as the ledger's row-3 cell says.
- **ADR-0011 opened** with its eight rows, windows and kill-dates; the
  terminus declared.
- **Exit**: the intake table signed; the draft promoted with its deviations;
  row 3 chosen, three candidates closed with reasons; the ledger opened; the
  terminus declared.

## Session 1 — the shelf baseline + the debt re-verification (~1 h)

- **Row 5 (shelf baseline, year five)**: the hostile-webmaster walk of the
  live site + Space health at zero (links, assets, a11y, orphans); the claims
  gate re-run on the live surface — a live public number never exceeds the
  record.
- **Row 8 (debt re-verification)**: W&B, clean-clone proof, graduation proof,
  `reproduce-multiseed` exp2/exp5, and the exp5 1000-epoch ×3-seed
  resolution (which ADR-0010's row 8 must have dated) — each cell re-verified
  LAUNCHED (with its transcript re-checked) or CLOSED (with one named
  reason); a claimed closure without its transcript stays open and blocks
  Session 8.
- **Exit**: rows 5 and 8 stamped; the hostile-webmaster transcript at zero;
  the gate-debt transcript file updated.

## Session 2 — the consumed-verdict sitting (rows 1, 2) (~1–2 h)

- **Row 1 (the fifth research question consumed as artifact — consumes
  ADR-0010 row 3's verdict)**: the executed question's verdict (or its
  scheduled negative) becomes the paper-v7 section, the annex table, or the
  results-page row — every number manifest-tagged, consumed in the sitting
  that owns it. If MP-36 ran C18: the order-parameter section with its
  boundary figure as a provenance-guarded artifact. If C17: the sparse
  mechanism section with its per-frequency dictionary. If C19: the
  reproducibility-rate row.
- **Row 2 (the paper v7 rule)**: v7 opens only if row 1 lands new numbers;
  else the row closes with the dated reason "the v6 is the record" and
  `make paper` is re-verified green (or graceful-no-toolchain) in the CI
  mirror against v6.
- **Row 6's substitute filed** (from the visitor's chair, before the window
  opens).
- **Exit**: row 1 dated and manifest-tagged; row 2 dated either way; the
  substitute filed.

## Session 3 — the essay annex v7 (~2–3 h)

- **Row 4 (`portfolio/essay-annex-7.md`)**: the fifth question's verdict set
  and the teaching lane's fourth receipt distilled into one dated annex —
  every number manifest-tagged, the reverse claims audit at zero (prose →
  manifest → command), each claim's "what would falsify this" column filled
  at writing time. The annex is never rewritten afterwards, only amended via
  dated annexes.
- **Exit**: row 4 dated; the reverse claims audit at zero; the essay cites
  only disk.

## Session 4 — the stranger round 7 intake (~1 h)

- **Row 6's window opens** (intake S4, kill-date S5). The feedback-to-fixes
  matrix is pre-stamped: friction point → cause → dated fix → re-check row.
- **Exit**: the window open with its kill-date declared; the substitute on
  file; the matrix's column headers committed.

## Session 5 — the research row pre-registration + launch + the teaching lane kickoff (~2–3 h)

- **Row 3 (the sixth research question)**: the chosen candidate's protocol
  written before the first pass — site, metric, negative control, kill-date,
  the "what would falsify this" column at writing time — and the run launched
  under a heartbeat while it is live. The verdict (or the scheduled negative
  from the manifest) is the row's result, manifest-tagged.
  - If C21 was chosen: the per-head dictionary and norm-structure claims
    written as falsifiable predictions before a single number is read — the
    dictionary's support, the norm split vs the ideal DFT expansion, the
    ablation's effect profile; the negative control is the frozen protocol
    itself; the dictionary figure's spec written before the analysis.
  - If C22 was chosen: the sharpening-with-P and monotonicity predictions
    written before the analysis; the edge-of-stability check on the
    dense-regime cells; the boundary figure's spec written before the
    analysis.
- **Row 6's kill-date**: no feedback by this sitting → the row closes with
  the S2 substitute; feedback present → the matrix drafted here, fixes land
  with dates.
- **Row 7 (the teaching lane) kicks off**: the fifth teaching artifact's
  skeleton drafted — the walkthrough v5, the 10-minute talk v5, or the Colab
  grokking notebook v3 — with its ship-date.
- **Exit**: row 3 pre-registered and launched with its heartbeat live;
  row 6 closed or drafted at its kill-date; row 7's skeleton drafted with its
  ship-date.

## Session 6 — the research verdict sitting (~1–2 h)

- **Row 3's verdict**: read from the manifest — the run completed and the
  verdict is dated, or the window closed and the scheduled negative is the
  result. Either way the row is stamped in this sitting; the unchosen
  candidates stay closed.
- **Exit**: row 3 dated either way.

## Session 7 — the shelf rehearsal + the re-check row + the teaching polish (~2–3 h)

- **Row 5 rehearsal**: the hostile-webmaster walk at zero on the live site +
  the Space health check; the claims gate re-run beside the browser, every
  public number clicked back to disk.
- **Row 6's re-check row**: the fixes matrix re-read in the same sitting as
  the fixes (feedback present), or the substitute's closure re-confirmed
  (silent window) — row 6 dated either way.
- **Row 7 (the teaching lane) polished and shipped**: the fifth artifact
  runs end to end on a stranger's machine (fresh clone / Colab session), the
  transcript of that run is the receipt. The teaching distillation exercise
  (Ex-F) lands here.
- **Exit**: row 5 dated or closed; row 6's re-check row dated; row 7 shipped
  with its run-transcript; the walk at zero.

## Session 8 — the release (~1 h)

- ADR-0011 at **zero UNDECIDED rows** — each LAUNCHED-with-date or
  CLOSED-with-one-reason; the continuum law honored (exactly one research row
  opened, its verdict or scheduled negative in the ledger); the merge green
  locally and on GitHub; `dev == main`; home wired — this roadmap wired into
  home at release, its draft status retired; this roadmap archived with its
  deviations — every deviation a dated ledger note.
- **Exit**: the merge; `dev == main`; the ledger is the phase's after-the-fact
  truth; the program has its sixth dated direction.

## Gate criteria (pre-registered, from the sessions above)

1. S0: intake table signed (MP-36's release: ADR-0010 zero UNDECIDED + the
   live URL re-clicked + `verify-claims` at its actual count + the fourth
   teaching transcript on disk); the draft promoted from the release report
   with its deviations recorded; the continuum choice made — exactly one
   row 3 opened, three candidates closed with dated reasons in the same
   sitting; ADR-0011 opened; the terminus declared.
2. S1: row 5's baseline stamped; row 8 re-verified (each cell
   LAUNCHED-with-transcript or CLOSED-with-reason, including the exp5
   1000-epoch resolution); the hostile-webmaster transcript at zero.
3. S2: row 1 dated (manifest-tagged artifact of the fifth research
   question); row 2 dated (v7 opened or "the v6 is the record").
4. S3: row 4 dated (annex v7; reverse claims audit at zero).
5. S4: row 6's window open with its kill-date declared; the substitute filed.
6. S5: row 3 pre-registered + launched (protocol before the first pass,
   heartbeat live); row 6 closed or drafted at its kill-date; row 7's
   skeleton drafted.
7. S6: row 3's verdict dated or closed.
8. S7: row 5 dated or closed; row 6's re-check row dated; row 7 shipped with
   its run-transcript; the walk at zero.
9. S8: ADR-0011 zero UNDECIDED rows; the merge green; `dev == main`; home
   wired; this roadmap archived with its deviations.

## The one measured line (status)

ADR-0011 at **zero UNDECIDED rows** on release day, with exactly one LAUNCHED
research row (row 3) whose verdict (or scheduled negative) re-derives from a
manifest; `verify-claims` at 0 with every public number re-derivable from one
command line; the hostile-webmaster walk at zero on the live shelf, year
five; the fifth teaching artifact shipped with a stranger-runnable
transcript; `dev == main` and the program's sixth dated direction, stamped in
the same sitting as the merge.

## Deep-dive study plan

1. **The dense algorithm's computation (the C21 reading)**: per-head Fourier
   dictionaries, norm structure vs the ideal full-support DFT expansion of
   the addition table, QK/OV composition, frequency ablation — reading the
   P=113 checkpoints as code, with MP-29's S3 characterization as the
   framing and the C13/C14 verdicts as the record's own map of the dense
   regime.
2. **Phase boundaries and finite-size scaling (the C22 reading)**: order
   parameters, critical lines, boundary width vs P — the
   statistical-mechanics vocabulary applied to the dense→memorized boundary;
   Morwani et al., 2024 (the edge-of-numerical-stability regime), Nanda et
   al., *Progress Measures for Grokking via Mechanistic Interpretability*
   (2023), Gromov, *Grokking: A Memory Perspective* (2023), Power et al.,
   2022 — studied as theory before the analysis is fixed, exactly as the
   phase diagram's axes were studied before its sweep grid.
3. **Shortcut learning and universality** (Liu et al., *Transformers Learn
   Shortcuts to Automata*, 2023; Chughtai et al., *A Toy Model of
   Universality*, 2023): what makes a solution a shortcut vs a canonical
   algorithm — the C10 verdict consumed as framing for whichever regime the
   diagram found.
4. **SAE failure modes where the circuit is dense** (Bricken et al., 2023;
   Cunningham et al., 2024): L0/FVE/dead-features as regime labels, not just
   diagnostics — the C24 reading.
5. **Reproducibility science (the C23 reading, sixth pass on Gelman &
   Loken)**: how a study of receipts is designed — failure-rate baselines,
   per-step pass/fail definitions, and what a reproducibility rate actually
   claims; the record's standing countermeasure applied to its own showcase.
6. **The record teaches, round five**: the sixth question's verdict in four
   registers — the paper's sentence, the annex's sentence, the 30-second
   spoken claim, and the 5-minute teaching explanation with a worked toy a
   stranger can run; the gap between the last two is where my teaching leaks.
7. **Negative results as maps, deepened**: how a characterized boundary is
   reported honestly — critical lines, order parameters, falsified
   predictions — and how the paper presents a mapped negative as a
   contribution; the dense characterization as the record's strongest form
   of the signature, now read at the mechanism level.

## Documentation contract

- This draft (the file you are reading) + the roadmap it becomes at Session
  0, rewritten from MP-36's release report with its deviations recorded.
- ADR-0011's eight rows stamped with dates and verdicts — LAUNCHED or CLOSED,
  nothing "awaiting"; rows 1–2 consumed from ADR-0010's verdicts; row 3 the
  sixth research question with its protocol note and heartbeat; rows 4–8 the
  continuum law's decisions.
- `portfolio/essay-annex-7.md` — the v7 annex, manifest-tagged, never
  rewritten after release, only amended via dated annexes.
- The paper v7 diff (`portfolio/paper/main.tex` v7 + diff log) or the dated
  "the v6 is the record" memo; `make paper` re-verified in the CI mirror.
- The shelf health sheet + hostile-webmaster transcript (site + Space at
  zero, year five); the claims gate re-run on every merge.
- The gate-debt re-verification transcript (`checklists/gate-debt.md`):
  each cell's transcript or one-line reason, dated in S1 — including the exp5
  1000-epoch ×3-seed resolution's receipt (dated by ADR-0010's row 8,
  re-checked here).
- The research row's pre-registration note (site, metric, negative control,
  kill-date) in `06_production_ai/notes/` + the heartbeat artifact; if C21:
  the per-head dictionary figure spec before the analysis, the figure itself
  manifest-tagged after.
- The fifth teaching artifact + its stranger-run transcript (fresh-clone or
  Colab session receipt).
- `00_meta/03-progress-log`: one dated entry per session; this roadmap wired
  into home on release (the draft itself is not wired — it is not a
  pre-registration); the continuum ledger's rows cited by the skill tree's
  publication flips.

## Practical exercises and challenges

1. **Ex-A · The C21–C24 adjudication drill (S0):** each candidate's
   opening-or-closure memo in three sentences with a falsifier; decide
   exactly one to open; the unchosen close with one dated reason, stamped in
   the same sitting.
2. **Ex-B · The consumed-verdict reverse audit (S2):** every number from
   ADR-0010's row-3 verdict traced to its manifest and its command; the rest
   struck with a reason — the hostile-webmaster test of my own prose, fifth
   run.
3. **Ex-C · The order-parameter hand-roll (S5):** if C21 or C22 opens, the
   question's predictions written as falsifiable statements before the
   analysis runs — the dictionary's support, the norm split, sharpening with
   P — the "what would falsify this" column filled at writing time.
4. **Ex-D · The scheduled negative drafted before the run ends (S5):** the
   negative written while the run is live, so the S6 verdict sitting is a
   stamping, not a discovery.
5. **Ex-E · The hostile-webmaster walk v7 (S7):** the live site + Space at
   zero — links, assets, a11y, orphans, dead figures — walked as a complete
   transcript, year five.
6. **Ex-F · The teaching distillation, round five (S7):** the sixth
   question's verdict in four registers — the paper's sentence, the annex's
   sentence, the 30-second spoken claim, and the 5-minute teaching
   explanation with a worked toy a stranger can run; the gap between the
   last two is where my teaching leaks.
7. **Ex-G · The stranger substitute from the visitor's chair (S2, before the
   window opens):** the self-review written from the chair a stranger would
   occupy — friction points → fixes → re-check row — filed before S4, so the
   S5 kill-date can never close the row with a skip.
8. **Ex-H · The fork drill (S2, verdict-agnostic):** the redemption (a sparse
   cell found) vs the all-dense future written as two one-page paths — what
   each verdict changes downstream, including the C21-vs-C22 choice — so the
   S0 choice next phase is a stamping, not a discovery.
9. **Habit · The clock check (every session):** ADR-0011's undated rows, the
   open PR's CI status line, the shelf's health — all three before any new
   prose.

## Strategic tips and architectural best practices

- **The one-question law, sixth execution.** A phase that opens two research
  questions is drift by another name; the unchosen candidates close in the
  same sitting as the choice. The continuum law is the mechanical refusal of
  this drift — proven executable five times, it must simply be executed
  again.
- **The candidate set is frozen before S0, never improvised at it.** C21–C24
  are frozen in this file, conditioned on MP-29–MP-36 verdicts; a sitting
  decides, it never invents. The unchosen close with dated reasons in the
  same sitting as the choice.
- **Consumption is execution.** A verdict consumed into an artifact in the
  same sitting is a result; consumed into a paragraph written later it is a
  memory. Row 1 consumes ADR-0010's row-3 verdict in the sitting that owns it.
- **The record teaches, and the receipt compounds.** The fifth runnable
  artifact is only worth shipping because the first four transcripts proved
  the format — and if C23 opens, the receipts stop being anecdotes and
  become a measured rate. The showcase's story is now "read it, run it,
  watch me be wrong on the record," five receipts deep.
- **Verdict-agnostic by law.** This draft re-plans nothing of MP-29–MP-36;
  its candidates are conditions, not predictions. The fork drill (Ex-H) keeps
  both futures written without pre-committing to either.
- **The shelf is a row, not a mood.** Year five begins with a heartbeat and a
  hostile-webmaster transcript; a live public number that exceeds the record
  is a blocked row, not a to-do.
- **Annexes are amended, never rewritten.** Annex v7 inherits the annex
  doctrine: a new annex appears via a dated fork, the original preserved.
- **The S0 gate is a checklist with receipts.** MP-36's release report
  (ADR-0010 at zero, the live URL, `verify-claims` at 0, the fourth teaching
  transcript on disk) is a condition with artifacts, not a paragraph — the
  stacked stack without execution would be the permanent warning made real,
  and Session 0 refuses it mechanically.
- **The negative stays the signature.** The row that closes with one reason
  is stamped like the row that launched; the sixth question earns its verdict
  or its scheduled negative with equal polish — and if C21 opens, the dense
  mechanism reading is the strongest form of the record's signature: a
  negative that became a map, a map that became a characterization, and a
  characterization that became a mechanism.
- **The debt row re-verifies; it never re-does.** A stamped closure is
  re-checked with its transcript; a genuinely new debt cell is a NEW row,
  never a revision. A pending item cannot outlive a ledger.
- **The un-cap is the reward, not the norm.** The pipeline cap's intended end
  state is a roadmap written from a release report — this draft exists so
  that end state lands as a stamping, executed exactly once, never as a
  pre-registration in disguise.
- **Architecture laws unchanged.** `dev` only, GPG-signed, Conventional
  Commits with `(meta)`, `(portfolio)`, `(ci)` scopes; CI green before any
  merge; the floor re-verified locally before every push; zero UNDECIDED rows
  at Session 8; release = merge + 14 calendar days.
- **The showcase 30-second story**: *the program's sixth dated direction was
  written from its own release report — the cap honored, the stack executed,
  the record taught five times in runnable artifacts, and every public
  number still re-derives from one command line.* Every artifact this phase
  launches is written to that standard.

## Links

- [[00_meta/35_micro-phase-36-the-fifth-question]] — the roadmap this draft
  conditions on; its release report and ADR-0010 stamps are Session 0's
  starting artifact.
- [[docs/adr/0010-continuum-ledger-5]] · [[docs/adr/0009-continuum-ledger-4]] ·
  [[docs/adr/0008-continuum-ledger-3]] · [[docs/adr/0007-continuum-ledger-2]] ·
  [[docs/adr/0006-continuum-ledger]] — the machines whose verdicts reopen here
  as NEW rows under ADR-0011's dates; ADR-0011 itself is opened at Session 0,
  when this draft is promoted from MP-36's release report.
- [[06_production_ai/notes/results-manifests-and-provenance]] — the manifest
  machinery every public number cites.
- [[06_production_ai/notes/dense-solutions-modular-addition]] ·
  [[06_production_ai/notes/positive-control-protocol]] ·
  [[06_production_ai/notes/microscope-trial-table]] — the science C21–C24
  adjudicate over (MP-29's S3 characterization note joins them on disk; the
  phase diagram, MP-35's C14 verdict, extends the map).
- [[07_capstone/research-plan]] — the research plan the paper, the essay and
  the teaching lane distill.
- [[00_meta/03-progress-log]] — the dated journal entries per session,
  including the rows that closed and the launches that failed (the MP-12
  lesson, applied to URLs and now to runnable artifacts).